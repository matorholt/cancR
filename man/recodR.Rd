% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recodR.R
\name{recodR}
\alias{recodR}
\title{Recode multiple variables}
\usage{
recodR(data, namelist, match = "exact", replace = F, dt = F)
}
\arguments{
\item{data}{dataframe}

\item{namelist}{list og variables with named vector(s) containing diagnosis codes (e.g. list(var1 = list(name = diagnosis codes)))}

\item{match}{Whether the provided diagnosis codes should be matched exactly, start with/end with, be bounded by or contain (default)}

\item{replace}{whether multiple matches should be replaced directly (such as 1,4 to head, arm)}

\item{dt}{whether the dataframe should be returned as data.table (default = F)}
}
\value{
the input data frame with recoded variables
}
\description{
Recode multiple variables
}
\examples{

            df \%>\%
  recodR(list("diag" = list("KOL" = "DX123",
                            "Astma" = c("DC123", "DC2"),
                            "AMI" = c("DG123", "DG234"))),
         match = "exact")
df \%>\%
  factR(c(diag, type, type2)) \%>\%
  recodR(list("diag" = list("KOL" = "DX123",
                            "Astma" = c("DC123", "DC234"),
                            "AMI" = list("DG123", "DG234"))),
         match = "exact")

df \%>\%
  recodR(list("split" = list("one" = "1",
                             "four" = "4",
                             "ten" = "10",
                             "fifteen" = "15")),
         match = "boundary")

df \%>\%
  recodR(list("split" = list("one" = "1",
                             "two" = "2",
                            "eleven" = "11",
                             "four" = "4",
                             "ten" = "10",
                             "fifteen" = "15")),
         match = "boundary",
         replace=T)

}
