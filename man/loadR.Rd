% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loadR.R
\name{loadR}
\alias{loadR}
\title{Load registers}
\usage{
loadR(
  regs,
  pattern.list = NULL,
  pattern.custom = NULL,
  n = NULL,
  id.filter = NULL,
  keep.list = NULL,
  vars.list = NULL,
  lmdb.start = 1995,
  lmdb.stop = 2023,
  simulation = F,
  cores = 4,
  dt = F,
  gb = NULL,
  cancR.covariates = "main",
  ...
)
}
\arguments{
\item{regs}{which registers should be loaded. Default is all (lpr, pop, pato, cancer, lmdb and opr)}

\item{pattern.list}{named list of vectors of diagnoses codes for each register in the format ("lpr" = c("DC92", "DC21")). If multiple columns should be searched, an extra list layer is added ("lpr" = list("diag" = c("DC1"), "tildiag" = "DC2"))}

\item{pattern.custom}{named list for custom filter expressions}

\item{n}{number of observations that should be loaded}

\item{id.filter}{optional possibility to limit the registers to a defined patient population of PNRs}

\item{keep.list}{which variables should be kept provided as a list("lpr" = c("vars")).}

\item{vars.list}{which columns should the pattern filter be applied to. Defaults to diag, atc, opr and c_morfo3,}

\item{lmdb.start}{first year of LMDB}

\item{lmdb.stop}{last year of LMDB}

\item{simulation}{whether the registers should be simulated}

\item{cores}{number of cores for parallel processing}

\item{dt}{whether the returned data should be in data.table format}

\item{gb}{max size for future options}

\item{cancR.covariates}{which covariates that should be loaded. Options are: main (non-major), major (only major), none (no comorbidities) and all.}

\item{...}{arguments passed to simulatR()}
}
\value{
Returns the selected registers to the global environment
}
\description{
Simple loading function of the most used registers including variable selection, regex filtering and id_list filtering.
}
